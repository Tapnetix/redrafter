// Vitest setup: registers @testing-library/jest-dom matchers (toBeInTheDocument,
// etc.) for React component tests. Wired via test.setupFiles in vitest.config.ts.
import '@testing-library/jest-dom';
