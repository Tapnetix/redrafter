// Standalone route so the Connections screen (app/src/screens/Connections.tsx)
// can be visited directly in isolation, matching the pattern in
// app/src/app/general/page.tsx.
import Connections from '@/screens/Connections';

export default function ConnectionsPage() {
  return <Connections />;
}
